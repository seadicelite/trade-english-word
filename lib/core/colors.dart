import 'package:flutter/material.dart';

/// SEADICE Core Colors
/// ⚠️ 全プロジェクト共通・原則変更禁止
class sc {
  // ===== Brand =====
  static const Color ss = Colors.lightGreen; // Splash専用
  static const Color text = Color(0xFFF5F5F5);
  static const Color icon = Color(0xFFEEEEEE);
  static const Color back = Color(0xFF202020);
  static const Color appbar = Color(0xFF101010);
  static const Color button = Color(0xFF303030);
  static const Color card = Color(0xFF424242);

  // ===== Meaning Colors (使い道固定) =====
  static const Color success = Colors.grey; // ○ 正解
  static const Color error = Colors.blue; // × 不正解
}
