import 'package:flutter/material.dart';

/// SEADICE Core Colors
/// ⚠️ 全プロジェクト共通・原則変更禁止
class sc {
  // ===== Brand =====
  static const Color ss = Color(0xFF4EA1FF); // Splash専用
  static const Color text = Color(0xFFEDEDED);
  static const Color subttext = Color(0xFF9E9E9E);
  static const Color border = Color(0xFF2A2A2A);
  static const Color icon = Color(0xFFEEEEEE);
  static const Color back = Color(0xFF0F0F0F);
  static const Color appbar = Color(0xFF1E1E1E);
  static const Color button = Color(0xFF1E1E1E);
  static const Color card = Color(0xFF1E1E1E);

  // ===== Meaning Colors (使い道固定) =====
  static const Color success = Color(0xFFEF4444); // ○ 正解
  static const Color error = Color(0xFF4EA1FF);
  static const Color splash = Color(0xFF22C55E); // × 不正解
}
